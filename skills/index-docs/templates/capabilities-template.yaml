# capabilities.yaml template
# Auto-generated by /index-docs skill
# This file maps module-level capabilities for AI agent navigation

ecosystem: "{ecosystem}"
docs_url: "{docs_url}"
doc_type: "{sphinx|pkgdown|custom}"
last_sync: "{YYYY-MM-DD}"

modules:
  # Example module entry
  preprocessing:
    url: "https://example.readthedocs.io/en/stable/api/preprocessing.html"
    purpose: "Brief description of what this module does"
    typical_workflow_position: "When in analysis workflow this is typically used"
    covers:
      - task or capability 1
      - task or capability 2
      - task or capability 3
    when_to_consult: "Natural language: when should an agent look here?"
    function_count: 25
    key_functions:
      - function_name_1
      - function_name_2
      - function_name_3

  # Additional modules follow same structure
  tools:
    url: "..."
    purpose: "..."
    typical_workflow_position: "..."
    covers: []
    when_to_consult: "..."
    function_count: 0
    key_functions: []